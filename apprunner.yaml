version: 1.0
runtime: python3 
build:
  commands:
    build:
      - pthon3 -m venv env
      - env\scripts\activate
      - sudo apt install python3-pip
      - pip install Flask
      - pip install jsons
run: 
  command: python app.py
